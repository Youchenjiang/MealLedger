export function App() {
  return (
    <main className="app-root">
      <section className="app-card">
        <p className="eyebrow">MealLedger</p>
        <h1>V1 app shell</h1>
        <p>
          The React shell is ready for navigation, auth states, and feature placeholders.
        </p>
      </section>
    </main>
  );
}

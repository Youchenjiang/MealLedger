# 口說新增計畫（Phase 3）

狀態：**計畫，尚未實作**（分支 `feature/voice-capture`，預期獨立 PR）。本文件為版面重構（底部導覽決策見 [ADR 0006](../decisions/0006-unified-bottom-navigation.md)）中新增頁（Phase 3）的拆分細節。

---

## 1. 目標

- 「新增」頁（現 `/capture`）以**口說記帳**為主流程；手動填表、掃描、照片等其他方式降為附屬。
- 版面由上而下為**欄位區塊**（「一籃一籃」），狀態一目了然（已填入顯示值 / 待填灰底）。

## 2. 欄位區塊

- 頁面上方依序排列每個需要填的欄位區塊，**順序已定案**：`日期 → 類型 → 帳戶 → 類別 → 對象 → 品項 → 金額`。
- 每個區塊顯示狀態：已填入（顯示值）/ 待填（灰底）。

## 3. 兩種口說模式（頁內切換）

- **模式 A：整段口說**——使用者一次念完整筆（例：「7/25 中午和同事吃牛肉麵 480 現金」），系統解析後**自動填入對應欄位區塊**，使用者逐欄確認後存檔。
- **模式 B：逐步口說**——欄位**一個一個亮起**（含提示文案，如「請說金額」），使用者逐欄念出、逐欄填入，最後確認存檔。有設定 AI 金鑰時，每一欄的語音辨識結果可再**經 AI 修正成該欄位所要的格式**（如「兩百五十」→ `250`、「七月二十五」→ `2026-07-25`、帳戶/類別模糊比對），修正後仍顯示給使用者確認才寫入；無 AI 金鑰時直接寫入原始辨識結果。

技術基礎（可重用）：

- 模式 A 直接沿用 `AiLedgerPanel`：Web Speech API（`zh-TW`）把語音轉成文字 → AI 解析 → 草稿 → 使用者確認 → 寫入正式記錄（維持「AI 只產草稿、確認才算數」的既有邊界）。
- 模式 B 預設不依賴 AI：語音辨識結果直接寫入當前亮起的欄位，離線/無 AI 金鑰時仍可用。當 AI 已設定時，逐欄結果可選用同一條 AI 管線（`requestAiJson` + 單欄位 prompt）修正成該欄位的格式後再填入——**AI 修正為可選增強，不是必要路徑**。本地的離線 AI 模型路徑（模式 A/B、發票 OCR 各自獨立離線運作）另見 [Offline AI spec](../specs/offline-ai/requirements.md)（V2+ 計畫）。
- 發票/收據照片、餐點照片、附件流程全部保留，只是降級為附屬入口（「其他方式」側邊/收合區）。

## 4. 影響範圍

| 檔案 | 預期變動 |
|---|---|
| `src/ai/AiLedgerPanel.tsx` | 拆分/擴充：欄位區塊狀態、模式 B 逐步口說 |
| `src/App.tsx` | Capture 頁版面：欄位區塊 + 兩種模式切換 + 「其他方式」收合區 |
| `src/styles.css` | 新增頁欄位區塊樣式 |
| `src/App.test.tsx` | 新增口說模式與欄位區塊測試 |

## 5. 已定案

- 口說欄位順序：日期 → 類型 → 帳戶 → 類別 → 對象 → 品項 → 金額。
- 模式 B 的 AI 修正為**可選增強**：有 AI 金鑰時逐欄把語音結果修正成欄位格式（經使用者確認後填入），無 AI 金鑰時退回原始辨識結果，兩種環境都能完成逐步口說。

## 6. 驗證

`npm run typecheck`、`npm test`、`npm run build`、`npm run test:e2e`；模式 B 在無 AI 金鑰環境可用。
